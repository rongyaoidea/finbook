# FinBook 财务管理系统 — 使用说明

> 金蝶 / 用友风格的桌面财务软件，单文件账套，Rust 编写。
>
> 当前版本覆盖：核心闭环（账套 → 科目 → 期初 → 凭证 → 账簿 → 报表 → 期末结账）+ **期末四件套**（固定资产 / 银行对账 / 往来核销 / 月度自动化）+ **业务三件套**（存货 / 工资个税 / 费用报销）+ **管理会计三件套**（预算 / 多维损益 / 自定义报表）+ **资金管理**（票据 / 融资 / 资金日报 / 资金预测）+ **成本核算**（计价方式配置 / 期末结价）+ **供应链全生命周期**（采购 / 销售 / 库存深度）+ **生产制造**（MRP / 工艺路线 / 工序报工）+ **安全中心**（口令策略 / 账户锁定 / 会话超时 / 登录审计）+ **出纳作业台**（出纳日记账与日清、现金盘点、支票簿、员工借支、票据融资台账联动凭证、按日资金日报、资金预算）+ 凭证模板 / 常用摘要 / 电子附件。

---

## 1. 安装与启动

| 平台 | 文件 | 大小 | 依赖 |
|------|------|------|------|
| Windows 10/11 x64 | `target/x86_64-pc-windows-gnu/release/finbook.exe` | ~12 MB | 无（标准 Windows DLL） |
| Linux x64 | `target/release/finbook` | ~17 MB | xkbcommon, fontconfig（系统已带） |
| macOS | `cargo build --release` | — | — |

启动方式：

```bash
# 不带参数：进入"请选择账套"页
./finbook

# 带账套路径：直接打开该账套（仍需在登录页输入用户名密码）
./finbook /path/to/company.fbk
```

> 账套就是一个 `.fbk` 文件（标准 SQLite 库），备份只需复制该文件。

### 1.1 在服务器上多用户共享同一账套

`.fbk` 是标准 SQLite 文件，**可直接复用已有服务器上的账套**，无需迁移数据。

- 启动时已启用 `journal_mode = WAL` + `busy_timeout = 5s` + `synchronous = NORMAL`，
  因此**同一台服务器上多个进程 / 多个登录用户可同时打开同一个 `.fbk`**，
  并发写入会自动等待而不会报 `database is locked`。
- 典型部署：把 `.fbk` 放在财务服务器的**本地磁盘**上，各会计人员通过远程桌面 / 终端服务
  在同一台服务器上各自启动 FinBook 登录同一账套；或在同一台机器上多开实例。
- 推荐做法：让 FinBook **运行在账套所在的服务器本机**。若把 `.fbk` 放在 NFS / SMB 网络
  共享盘上再被多台不同电脑同时打开，SQLite 的 WAL 模式在网络文件系统上不稳定，
  可能损坏账套——此时应改用"服务器本机运行 + 远程桌面"模式。
- 升级不破坏旧账套：旧版本账套打开时自动执行 schema 迁移（含账号设备绑定字段），
  原有用户、凭证、余额全部保留。

---

## 2. 核心闭环 8 步走通流程

以下用「示例科技有限公司，启用期间 2025-01」走通从建账到期末结账的完整业务。

### 2.1 登录与选账套

![01_login](screenshots/01_login.png)

- 启动后进入登录页，列出**最近打开**的账套
- 点击 **「新建账套」** 创建新账套，或 **「打开已有账套」** 打开现有 `.fbk`
- 输入 **用户名 + 密码** 登录（新账套内置管理员账号 `admin`，默认口令 `admin123`，首次登录强制改密）

> 登录受 **口令策略** 保护：连续错 5 次锁 15 分钟；闲置 30 分钟自动登出（详见 §4.4 安全中心）。
> **普通账号由管理员开通**：管理员在「系统 → 用户权限」新增账号后，该账号才能登录（详见 §6）。

### 2.2 首页 / 工作台

![02_dashboard](screenshots/02_dashboard.png)

| 卡片 | 含义 |
|------|------|
| 本期凭证 | 当期已录入的凭证数量（已记账 X） |
| 待处理 | 草稿数 · 待审核数 |
| 本期发生额 | 借/贷方汇总 |
| 期末余额 | 各科目期末借/贷余额汇总 |

下方有 **试算平衡**（期初 / 本期 / 期末三组）、**待办事项**（结账提示等）、**最近凭证**。

### 2.3 会计科目

![03_accounts](screenshots/03_accounts.png)

- 左侧导航 → **基础资料 → 会计科目**
- 工具栏：查找、只看末级、显示停用、刷新、**新增**、**导入内置科目表**、导出 Excel / CSV
- 列：科目编码 / 科目名称 / 类别 / 方向（借/贷）/ 辅助核算 / 数量 / 外币 / 现金 / 银行 / 状态

> **重要约定**：**末级科目**才能录入凭证，父科目仅用于汇总。

### 2.4 期初建账

![06_begin](screenshots/06_begin.png)

- 左侧导航 → **期初建账**，「启用期间」之前的业务在此录入
- 工具栏：保存 / 刷新 / **添加科目** / **按科目表补全** / 查找 / 只看有余额的 / **按期末余额倒推** / 导出 Excel
- 公式：**启用期初 = 年初余额 + 累计借方 − 累计贷方**，顶部试算差额必须为 0

### 2.5 填制凭证

![05_voucher_new](screenshots/05_voucher_new.png)

- 顶栏 → **填制凭证**
- 头部：日期 / 凭证字（记/收/付/转）/ 凭证号（自动）/ 附件数 / 制单人
- 工具栏：**保存**、**保存并新增**、**增行**、**删空行**、**自动平衡**、**审核**、**记账**、反审核 / 反记账、上一张 / 下一张、打印预览、作废 / 恢复、删除、**常用摘要**、**📎 附件**
- 底部合计差额必须为 0 才能保存

**保存时的硬校验**：
1. 借贷平衡；2. 全部末级科目；3. 辅助核算必填；4. 数量/外币科目必填数量×单价/汇率；5. 摘要非空；6. 凭证号唯一。

**本次新增接线**：

| 功能 | 说明 |
|------|------|
| **常用摘要** | 右侧「常用摘要」弹窗：搜索框 + 按使用热度排序的 top50，点击即填入当前焦点行。**保存凭证时自动学习**：本次用过的摘要热度 +1，越用越准 |
| **电子附件** | 「📎 附件」面板：上传（系统文件选择器，≤256KB 内联存入账套，大文件自动落账套同目录 `.attachments/`，存 SHA-256 校验和）/ 打开 / 删除 / 重命名。删除凭证时附件一并清理 |

### 2.6 凭证查询

![04_voucher_list](screenshots/04_voucher_list.png)

- 工具栏：期间 / 凭证字 / 状态 / 科目 / 关键字 / 查询 / 重置 + 批量审核 / 记账 / 反审核 / 反记账 / 作废 / 删除 / 导出
- **审计 → 记账** 是标准流程；也可在「期末处理」对整期批量记账。

### 2.7 账簿查询

![07_ledger](screenshots/07_ledger.png)

- 三个页签：**明细账** / **总账** / **日记账**；科目选择器 + 期间范围 + 含下级 + 只含已记账
- **「只含已记账」默认勾选**（H-3 口径，两端一致）：行集与期初/滚动余额同一口径；
  取消勾选则按「含未记账（排除作废）」查看，草稿行可见且计入滚动余额
- 明细账逐笔滚算**方向余额**

### 2.8 科目余额表

![08_balance](screenshots/08_balance.png)

- 列：期初借/贷 / 本期借/贷 / 累计借/贷 / **期末借/贷**，底部合计行试算平衡 ✓
- 取数口径：**期初余额 + 已记账分录**；草稿与已审核未记账凭证不入余额（H-3 定案）

### 2.9 会计报表

![09_reports](screenshots/09_reports.png)

- 三个页签：**资产负债表** / **利润表** / **现金流量表**
- 资产合计恒等于负债 + 所有者权益；未结转损益时本期盈亏挂损益类科目，公式照常平衡
- 取数口径同科目余额表：**只统计已记账凭证**（H-3 定案）

### 2.10 期末处理

![10_period_end](screenshots/10_period_end.png)

- 工具栏：**结转损益** / **期末结账** / **反结账** / **数据维护** / 期间切换
- **结转损益**：损益类科目净发生额 → 4103「本年利润」；**年末结转**：4103 → 410401「未分配利润」
- 结转取数按**含未记账（排除作废）**：生成的结转凭证本身是草稿，含草稿取数才能在
  第二次调用时看到「已清零」而拒绝重复结转；结账前 checklist 要求全部记账，
  届时与已记账口径结果相同

### 2.11 辅助档案

![11_aux](screenshots/11_aux.png)

- 7 类：**客户 / 供应商 / 部门 / 职员 / 项目 / 存货 / 银行账户**，支持导入内置现金流量项目

---

## 3. 期末四件套（期末处理扩展）

### 3.1 固定资产（期末 → 固定资产）

四个页签：**资产卡片 / 计提折旧 / 折旧明细 / 资产台账**。

- **新增卡片**：编码、名称、类别、原值、残值率（0~1）、预计月数、启用期间、使用部门（辅助核算）、折旧科目映射
- **计提折旧**：选定期间一键计提全部在用资产，自动生成「借：管理费用—折旧 / 贷：累计折旧」凭证（`VoucherSource::AutoTransfer`）；已计提期间自动跳过，不重复计提
- **台账**：原值 / 累计折旧 / 净值实时滚算；支持清理（生成处置凭证）

### 3.2 银行对账（期末 → 银行对账）

三个页签：**对账单录入 / 自动勾对 / 余额调节表**。

1. 录入银行对账单（日期 / 摘要 / 收 / 付 / 余额）
2. **自动勾对**：按金额 + 方向 + 结算方式智能匹配企业日记账与银行对账单
3. 未达账项手工调整，**余额调节表**一键生成：企业账面余额 ± 未达 = 银行对账单余额 ± 未达

### 3.3 往来核销（期末 → 往来核销）

- 三个页签：**手工核销 / 自动核销 / 账龄分析**
- 应收（1122）/ 应付（2202）按客户/供应商逐笔核销，核销凭证自动生成
- **账龄分析**：30/60/90/180/180+ 天五段账龄，按辅助对象汇总

### 3.4 月度自动化（期末 → 自动化）

四个页签：**调汇 / 自动转账 / 检查清单 / 一键月末**。

| 页签 | 功能 |
|------|------|
| 调汇 | 期末按记账汇率重估外币货币资金/往来，汇兑损益凭证自动生成（`VoucherSource::FxAdjust`） |
| 自动转账 | 定义模板：结转成本、摊销费用、计提税金等，按公式（固定额 / 期末余额×比例 / 取对方科目发生额）+ 执行频率（每月/季/年）一键生成凭证 |
| 检查清单 | 结账前自动体检：损益未结转、草稿/待审凭证、借贷不平历史、未核销往来、未计提折旧、未对账银行等 |
| 一键月末 | 按「检查 → 计提折旧 → 调汇 → 执行自动转账 → 结转损益 → 记账 → 结账」顺序串行执行，逐步日志反馈 |

---

## 4. 业务三件套 + 管理会计三件套

### 4.1 存货核算（业务 → 存货）

- **出入库单**：采购入库 / 销售出库 / 其他出入库；出库行数量为负，成本由引擎计算
- **存货盘点**：盘点单按仓库**快照账面** → 录实盘 → 应用生成 其他入库/其他出库 流水 + **盘盈盘亏凭证**（借/贷 1901 待处理财产损溢，金额 = 差异 × 标准价；未配标准价只调流水）；草稿可删、已应用不可删（冲回做反向盘点），仓储作业权限（仓管员专属）
- **批次与库位**：存货档案配「保质期天数」→ 批号自动生成 `BT+日期+序号`（或手工指定）；**批次余额 = 库存流水按 (存货,批号) 汇总——与普通库存同一本账**；批次登记（其他入库/出库带批号、仓库、初始库位）；**FEFO 近效期先出推荐**（无效期批次按生产日期）；**临期预警**（默认30天窗口）；库位主数据（存储/拣货/隔离）；仓储作业权限
- **收发存汇总**：期初 / 本期入库 / 本期出库 / 期末，按存货 + 仓库汇总
- **成本核算**：**移动加权平均 / FIFO** 两种计价引擎，结转销售成本一键生成凭证

### 4.2 工资与个税（业务 → 工资）

- **工资表**：按部门/职员录入应发、三险一金、专项附加扣除
- **个税计算**：累计预扣法（计税基数 = 应发 − 三险一金 − 专项附加 − 5000×月数，首月 3% 起档），自动算出本期应扣个税
- **凭证生成**：计提工资 / 发放 / 扣个税 / 扣社保四组分录一键生成

### 4.3 费用报销（业务 → 报销）

- 状态机：**提交 → 审批 → 付款 → 完结**（可驳回），全程留痕（申请人 / 审批人 / 付款人）
- 审批通过后自动生成报销凭证（借：管理费用等 / 贷：其他应收款或银行）

### 4.4 安全中心（系统 → 用户权限）

| 页签 | 功能 |
|------|------|
| 用户管理 | 管理员**新增 / 停用 / 改密 / 重置绑定设备**普通账号，设定角色与细粒度权限；**强制改密**横幅（口令过期提示） |
| 我的口令 | 任意登录用户可在此**修改自己的口令**（验证旧口令 + 策略校验），被强制改密的用户在此完成首次改密 |
| 口令策略 | 最小长度、复杂度（字母+数字）、有效期天数、**连续错 N 次锁定 M 分钟**、会话空闲超时分钟 |
| 登录审计 | 每次登录 / 失败 / 锁定 / 超时登出全部留痕，可按用户/时间筛选 |
| 数据范围 | 按用户配置可见辅助对象（部份/全部），界面灰字提示逐步接入各账簿 |

> **默认口令策略**：≥8 位且含字母+数字、有效期 90 天、错 5 次锁 15 分钟、闲置 30 分钟自动登出。
> **默认管理员**：账套内只有 `admin` 一个内置账号（默认口令 `admin123`，首次登录强制改密）；其余账号由管理员在「用户管理」开通。

### 4.5 账号设备绑定（一人一设备）

为防止同一账号在多处同时登录，FinBook 在每次登录时采集**设备指纹**并绑定账号：

- **首次登录自动绑定**当前设备；之后**换设备登录会被拒绝**（提示"该账号已绑定设备 XXX"）。
- **管理员不受此限制**，可在任意设备登录，也不绑定设备。
- 员工换电脑时，由**管理员在「用户管理」点击「重置绑定」**解除原绑定，该账号即可在新设备重新登录。
- 设备指纹来源：Windows 读 `MachineGuid`、Linux 读 `/etc/machine-id`、macOS 读 `IOPlatformUUID`，
  兜底用「主机名 + 用户名」哈希；绑定的设备名显示在用户列表「绑定设备」列，方便管理员辨认。

### 4.6 导出管控（普通账户只能打印，不能导出）

报表与账簿的"导出 Excel / 导出 CSV"按钮**需要 `Export` 权限**，仅**系统管理员与财务主管**拥有：

- **普通账户（会计 / 出纳 / 审计 / 只读）看不到"导出"按钮**，界面显示灰字提示"无导出权限，仅可打印预览"。
- 所有能看报表的账号都有**「打印预览」按钮**：把数据生成一份带打印样式的 HTML，用系统默认浏览器打开并自动弹出打印对话框。
  打印走的是 HTML，**不会以文件形式落地**，满足"只能打印、不能把整表数据带出"的管控。
- 凭证的 Excel 导出同样仅管理员 / 财务主管可用；屏幕内的凭证打印预览人人可看。

### 4.7 管理会计三件套

| 模块 | 说明 |
|------|------|
| **预算**（管理会计 → 预算） | 按科目 + 期间编制预算额；执行分析：实际发生 vs 预算、执行率、超支标红 |
| **多维损益**（管理会计 → 多维损益） | 按部门 / 项目 / 客户任意维度切分收入成本费用，输出多口径利润表 |
| **自定义报表**（管理会计 → 自定义报表） | 报表设计器：行 = 取数公式（科目余额 / 发生额 / 辅助明细），保存后一键刷新预览、导出 |

### 4.8 凭证模板 + 常用摘要（凭证 → 模板）

- **凭证模板**：把常做的分录组合存成模板（含借贷方向、金额公式、辅助核算），到期（月/季/年）提醒，一键生成草稿凭证
- **常用摘要**：与凭证录入联动，热度自动排序（见 §2.5）

### 4.9 出纳作业台（出纳 → 资金管理）

出纳岗位的完整闭环（出纳角色默认 `填制凭证 + 出纳签字 + 账簿报表` 即可上岗，记账/审核仍归会计）：

| 能力 | 说明 |
|------|------|
| **出纳日记账** | 资金管理 → 日记账：现金/银行科目逐笔（仅已记账 H-3）+ 滚动余额；**日清标记**按科目按日核对封账；**收款/付款登记**一键跳转凭证录入并预填科目 |
| **现金盘点** | 账面按当日资金日报口径快照 → 实盘录入 → 差异一键生成盘盈盘亏凭证（对方科目 1901 待处理财产损溢）；账实相符记录可直接删除 |
| **支票登记簿** | 票号/类型/付款科目/收款人/金额，开出 ↔ 作废流转；纯备查簿不入账，删除无凭证联动 |
| **员工借支** | 建单 → 支付（借 其他应收款-员工 / 贷 资金，同事务出凭证）→ 核销（冲账费用 + 退回现金 / 贷 其他应收款），超额与状态机守卫、幂等 |
| **票据 / 融资台账** | 贴现、背书、兑付、融资到账与结清**同事务自动生成台账凭证**（存量台账可「生成凭证」回填），资金台账与总账不再脱节 |
| **资金日报（按日）** | 上日结余 / 本日收支 / 日末结存，按日翻页（仅已记账） |
| **资金预算** | 现金/银行科目的预算 vs 已记账实际执行（预算行在预算管理维护） |
| **出纳签字** | 账套参数开启后，涉现金/银行的凭证记账前必须出纳签字（凭证弹窗按钮 + 账套参数开关），日记账带签字列 |
| **报销支付落账** | 报销单支付即自动生成付款凭证草稿（默认贷 100201），手动「生成凭证」幂等返回同一张 |
| **收付款单** | 资金管理 → 收付款（Web + 桌面）：保存即出凭证并按往来单位对未清挂账 **FIFO 自动核销**（见 §4.10） |

---

### 4.10 供应链全流程（对标金蝶：订单 → 出入库 → 收付款 → 会计记账）

从「创建订单」到「出纳收付」再到「会计记账」的完整链路，关键节点**同事务自动生成凭证**：

| 环节 | 动作与卡控 | 自动生成凭证 |
|------|-----------|--------------|
| **订单创建** | 销售/采购单据页「新建订单」：明细行编辑器，行金额=数量×单价、税额=金额×税率（服务端计算），草稿自由改删 | — |
| **订单确认** | 草稿 → 已确认（可作废）；**客户信用控制**：客户档案「信用额度」（0=不限），确认时校验「已确认订单占用（总额−已收款）」≤ 额度，超限拒绝；草稿不占额度 | — |
| **发货执行** | 已确认才能发货/退货（草稿/作废拦截）；**按比例确认收入**：收入与税额 × 本次数量÷订购总量（发货封顶未发量、退货封顶已发量不超额冲回）；订单状态随发货进度自动推进 部分发货/已完成 | **借 应收账款(客户辅助) / 贷 主营收入 / 贷 销项税**；退货出反向冲回 |
| **入库暂估** | 采购到货登记执行进度；入库未票在「采购暂估」登记（须挂真实订单）→ 同事务出凭证；到票「冲回」出反向凭证（UPDATE 条件化幂等，重复/并发冲回不出票） | **借 存货材料科目 / 贷 应付账款(订单供应商辅助)**；冲回反向 |
| **收付款** | 收付款单：选类型/资金账户/往来单位/金额 → **保存为草稿（仅台账）→ 审核时**同事务出凭证 + 对该往来单位**未清挂账按日期 FIFO 自动核销**（超出留预收/预付腿，无挂账全额预收）；审核权 = 审核人/主管（出纳录单、审核人把关），撤审删凭证回草稿；执行坞「收款/付款」落待审核单，审核后出凭证 | **收款：借 资金 / 贷 应收；付款：借 应付 / 贷 资金** |
| **核销账龄** | 自动核销进往来核销（手工/自动/账龄同口径）；**删除凭证自动清理其核销配对**，收付款单须先删凭证才可删单 | — |
| **会计记账** | 以上均为**草稿凭证**（H-3 草稿不入余额），会计审核记账后进余额与报表；涉现金银行凭证按出纳签字开关（§4.9）把关 | — |

生成科目集中在**账套参数 → 业务凭证默认科目**：应收 / 应付 / 收入 / 销项税 / 暂估材料 / 默认资金账户（须为末级科目，Web 与桌面两端可改）。

暂不覆盖（业务边界）：信用催收与账龄预警、税务发票对接、单据→凭证**可配置规则引擎**（当前为固定生成器 + 科目可配）。

---

### 4.11 单据套打（订单 / 收付款单）与批量打印

- **字段可选 + 一键预设**：列表里点「打印设置」勾选要打印的字段与明细列（单号 / 日期 / 状态·类型 / 客户·供应商·往来单位 / 备注 / 制单人 / 存货编码 / 名称 / 数量 / 单价 / 税率 / 金额 / 税额 / 行备注 / 合计行 / 签章栏 / 资金账户 / 关联凭证号），服务端按 `fields=` 白名单裁剪，未勾选的列头与内容都不出现；**预设**：「默认全单」一键还原 /「送货单（随车·留数量藏价格）」**保留数量供收货清点**，自动去掉 单价·税率·金额·税额·合计·状态（跟车联不带价格）；
- **多尺寸纸张**：打印设置选 `A4` / `A5 二等分(210×148)` / `三等分(99×210)` / 自定义「宽x高 mm」；`@page` 尺寸、页边距与**分页预算随纸张重算**（A4≈42 行 / A5≈20 / 三等分≈28），窄纸（<120mm）自动压缩字号行高；字段与纸张选择保存在本机浏览器（localStorage，兼容旧存储）；
- **批量 + 充分利用纸张**：列表勾选「打印所选 / 批量打印」（未勾选 = 当前期间 / 全部单据），多张单据按**行单位预算**（单据固定开销 6 行）紧凑排进同页——放不下才换页、整单不劈开（`break-inside:avoid`）；「紧凑分页」可关，改一单一页。可见列宽归一化到 100%；
- **覆盖范围**：销售订单、采购订单、收付款单（Web 三处列表勾选批量 + 打印设置；桌面收付款页签「打印全部」走同一模板）、出纳日记账「打印日记账」；凭证套打（整期间批量）、账簿套打、报表打印沿用既有通道；
- **报价单转订单**：报价单审批后一键「转订单」→ 生成草稿销售订单（税率 0 可再改）并把报价单标记 `converted`（重复转换拒绝）——订单部从 报价 → 确认 → 执行 → 打印 全程闭环。

---

### 4.12 工作流可视化（对标金蝶审批流设计器）

- **节点-动作模型**：开始 / 审批 / 条件 / 消息节点 + 普通 / 驳回连线；**无出边的审批节点即流程终点**；驳回走 Shift+拖拽 画出的驳回连线或节点「驳回至」；参与人 = 角色多选（留空 = 需审核权限者可批）；**会签**：strategy=all 且参与人非空时每个参与角色各需一票（同人不可重复批，未满票停留当前节点并显示「会签 x/y」）；**条件分支**：连线条件审批时求值（`amount > 5000` 等字段比较，多条出线按序匹配、空条件兜底，配置错误立即报批）。
- **SVG 画布（原生 JS，不依赖框架）**：拖拽节点（20px 钉吸）、节点蓝点拖出连线、滚轮缩放、空白拖拽平移、Delete 删除、Ctrl+Z/Y 撤销重做、小地图、适配视图、右侧属性抽屉。
- **运行时**：对已发布流程的业务单据（报价 / 请购 / 报销 / 收付款）执行审批时**自动创建实例并逐节点推进**，到终态才执行原有业务批准（收付款 = 出凭证 + 自动核销）；「运行实例」页回放轨迹（谁在何时通过/驳回哪个节点）并可就地通过/驳回（服务端校验参与人与审核权限）。
- **向后兼容**：未配置发布流程 = 走原固定审批（默认流），既有行为完全不变；设计/发布需账套管理员（`SysOption`）。桌面端设计器为后续迭代（运行审批不受限）。

### 4.13 工厂生产链：下达 → 开工 → 领料 → 完工（业务单据同步凭证）

- **下达**：`POST /api/prod` 新建生产订单（状态=已下达）——补齐此前全系统无生产订单创建入口的缺口；「MRP」结果页每行「下达」按钮按计划量一键下达。
- **开工**：已下达 → 生产中（`prod_start` 条件更新防并发），完工入库的前置状态。
- **领料**：按 **BOM × 计划量 × (1+损耗)** 展开，同事务「扣库存 + 归集生产成本 + 出领料凭证」——借 500101 生产成本-直接材料 / 贷各物料科目（数量核算）；单价取最近采购价，**无采购价回退计价配置标准价**（与盘点/暂估同口径，错误文案不变）。
- **完工入库**：仅生产中订单（条件更新防重复）；同事务「入库 + 订单推进 completed + 完工结转凭证」——借 140501 库存商品（数量核算）/ 贷 500101~03 各要素（只出非零要素）。
- **BOM Web 化**：`GET/POST /api/bom`（ProductionOps 权限）+ 报工页「维护BOM」编辑器（子件/用量/损耗率），此前 BOM 仅 findb 内部可用。
- **工序报工页 = 生产作业台**：同一订单下拉内完成 开工 / 领料出库 / 完工数量（自动带未完工数）/ 完工入库，动作后状态即时刷新；全链测试 `production_issue_complete_flow`。

### 4.14 我的工作台（按岗位权限动态聚合，对标金蝶工作台）

- **入口**：默认首页「仪表盘」升级为「我的工作台」（导航与 Logo 文案同步），登录即见；账套状态固定卡（当前会计期间 / 凭证数 / 分录数 / 科目数）原样保留。
- **权限分域（零新权限位）**：`GET /api/workbench?periods=6|12|24`（门槛 Report=人人可进）按既有 Perm 动态拼装——**凭证**（VoucherNew/Audit：未记账/已记账/发生额）、**报表**（FinReport，web 层语句表注入：资产/负债/权益合计、本期营业收入、本期净利润）、**资金**（CashierSign：待审/收款/付款）、**销售·采购**（OrderOps）、**仓管**（Warehouse：临期批次/在库批次/未完成盘点）、**生产**（ProductionOps）、**成本**（CostOps：WIP 料工费）；**报销域**=本人视角人人可见；**审批域**=运行中流程实例人人可见。
- **我的待办**（计数 >0 才可点击跳转）：未记账凭证待记账、待审核收付款、待审批报价/请购、待确认销售订单、临期批次、未完成盘点、在产生产订单、我的报销（待审/驳回）、**待我审批的工作流实例**（节点参与人匹配 = intercept 同规则，`workflow::pending_for`）。
- **多期趋势**：近 6/12/24 期可切换，每域核心指标（发生额 / 收付款 / 订单额 / 采购额 / 出入库量 / 生产投入 / 完工量 / WIP / 我的报销额 / 资产 / 收入与净利润），复用原生 SVG `lineChartSvg`，零新依赖；空期间补 0。
- **测试**：findb `role_domain_gating_and_series`（admin 全域、仓管/订单/只读分域、期数序列断言）+ web `workbench_role_matrix`（端点矩阵 + 建未记账凭证后待办计数联动 + keeper/clerk 实账号分域）。

### 4.15 单据下推与追溯（对标金蝶 源单→目标单）

- **下推**：请购单审批后一键「下推采购订单」——生成单行草稿（品名/数量带出，**单价与供应商留空待补**），记录 `doc_link` 勾稽、请购置「已下推」；**支持拆单多次下推**（每次新订单 + 新链）。
- **追溯**：新表 `doc_link`（UNIQUE 源→目标）+ `GET /api/doc-links?kind=req|po|so&id=` 统一返回——**上游源单 + 下游/执行子单据**合并视图（采购：到货退货与付款流水；销售：发货退货与收款流水）；采购/销售订单列表新增「链」按钮弹出单据链面板。
- **执行进度**沿用流水累计（`po_receipt` / `so_shipment` 外键直连，不落冗余字段）；执行坞保留「填入订单 ID」快捷方式。
- **测试**：findb `push_requires_approval_and_chain_links`（草稿拒推/审批后推/ordered 回写/单价留空/双向链/拆单双链）+ web `doc_push_chain`（端到端：未审批400 → 下推 → 到货流水并入链 → 拆单两下游 → 非法 kind 400）。

### 4.16 便捷操作（对标金蝶日常操作习惯）

- **Ctrl+K 全局快速搜索**：任何页面唤起——凭证（摘要/凭证号，**复用凭证列表口径 → 数据范围同步生效**）、采购/销售订单与请购（单号/往来/品名，OrderOps）、我的报销（数据范围与列表页一致，`doc_in_scope`）；分组结果点击/回车直达目标页，260ms 防抖。
- **Ctrl+Enter 提交当前弹窗主按钮**：所有 modal 表单通用（保存/记账/审批免找鼠标）。
- **请购「复制上一条」**：常驻表单一键带出最近请购（品名/数量/备注），录单员连续录入免重复打字。
- **凭证批量记账**（既有）：列表勾选/全选 + 批量记账，成功/失败分组 toast。
- **测试**：web `quick_search_flow`（空词不搜、请购/凭证/报销三类关键词命中、无结果 200 空数组）。

### 4.17 库存作业深化（到货入库闭环 / 质检 / 形态转换 / 低库存预警）

- **到货即入库（采购链闭环）**：Web 采购到货此前**只做执行跟踪、不写库存流水**（`kind=purchase` 仅桌面手工单产生）——现 `po_receipt_with_stock` 同事务「执行行 + 采购入库流水」，价格=订单行单价、金额=qty×价——**这也是「最近采购价」（领料成本回退链）的真实数据源**；防呆：订单无行拒、**单价 0 拒**（0 价入库污染移动平均，下推后先补价）；退货 `po_return_with_stock` 同事务负流水 + **超退防呆**（本次 ≤ 累计净收货）；多行订单按首行品/价入库。
- **质检单（对标金蝶来料检验）**：`qc_order` 表 + 采购订单行「质检」按钮（Warehouse 权限）——合格留库，**不合格部分自动按订单单价退货**（负到货 + 负采购入库同事务），结论 pass/fail/partial 随不合格数推导；防呆：不合格 ≤ 检验数、不合格退货价 0 拒、超退拒。
- **形态转换单**：`form_convert` 源物料出库 → 目标物料入库（同数量一减一增，**金额按源存货移动加权成本平移**——等值转换，源无成本价拒绝；原数量口径会给目标存货留 0 价首入污染计价，已修）；组装拆卸页新增「形态转换」工具条；同物料/非正数拒绝；无总账凭证（存货内部结构调整）。
- **低库存预警**：`below_safety`（`item_plan` 安全库存 > 0 且现有库存 < 安全量）→ `GET /api/inventory/below-safety` + **工作台仓管域新卡片「低于安全库存」与待办**（点击直达仓库库存页）。
- **测试**：web `inventory_qc_convert_flow`（到货 +20 → 质检不合格 5 自动退货 → 15 → 超退 400 → 转换 5/10 → 同物料 400 → 端点 200）+ `doc_push_chain` 增补 0 价防呆与补价序列；README §9 质检条目同步收窄。

### 4.18 最近价带出与按单限额领料（采购/生产深度）

- **价格历史自动沉淀**：保存采购订单时正价行按 (存货, 供应商, 日期) upsert 进 `price_history`（既有表此前**无生产写入方**，本波接通）。
- **最近价带出**：`GET /api/procure/price-history?item=`（日期倒序）+ 采购订单编辑器行内联动——**单价为空/0 时填存货编码即自动带出最近采购价**（带 toast 注明供应商与日期），录单与下推补价场景共用。
- **按单限额领料**：生产订单领料增加累计校验——按 `生产领料 PO#单号` 流水计数，**已领过料再领 → 400**（v1：BOM 全量一次领齐；带数量的部分领与超额补料留待领料单流程迭代），杜绝重复领料重复归集重复凭证的风险。
- **测试**：web `price_history_suggest`（建单沉淀 9 元 → 次日 10 元倒序最新在前 → 缺 item 400）+ `production_issue_complete_flow` 增补重复领料 400 断言。

### 4.19 委外加工（对标金蝶委外管理，schema v23）

- **模型**：委外 = 生产的变体——`production_order` 加 `order_kind`（inhouse/outsourcing）与供应商两组列（DDL + `MIGRATE_V23` 存量账套自动补列），**BOM/领料/开工/状态机/完工结转全链复用**，零重复实现。
- **创建**：工序报工页「新建委外」弹窗（产品/数量/供应商）→ `POST /api/prod` 带 `kind:"outsourcing"`；订单下拉带【委外】前缀与供应商名。
- **加工费确认**：`POST /api/prod/:id/outsource-fee`（ProductionOps）——归集 `CostType::Labor` + 应付凭证 **借 500102 生产成本-直接人工 / 贷应付科目（供应商辅助，科目取账套 `biz_accounts.ap`）**；仅委外订单、金额>0、缺供应商拒；金额并入订单人工要素，**完工时随 140501 一并结转**（完工凭证贷 500102 即加工费）。
- **完整链路**：建单 → 维护 BOM → 领料出库（材料凭证同自制）→ 开工 → 确认加工费（应付凭证）→ 完工入库（结转 料+工）——与金蝶"委外发料/加工费/委外入库"三段式语义对齐。
- **测试**：web `outsourcing_flow`（委外建单字段回显 → 领料 200 → 开工 → 加工费凭证 借500102/贷应付 aux=S01 → 非委外单 400 → 完工结转 140501 借 500 = 料200+工300，三腿逐科目断言）。

### 4.20 存货核算 ↔ 总账对账（对标金蝶对账表，成本域收尾）

- **对账口径**：**库存侧** = `stock_move.amount` 按存货累计至期间（含期末结价写入的调整流水——领料等 0 价流水由结价补齐，**建议先执行期末结价再对账**）；**总账侧** = 存货辅助余额（`aux_balance(kind=item)`，账套首期→期间，方向已展开，**余额口径=已记账凭证**）。
- **报表**：`GET /api/cost/gl-reconcile?period=`（CostOps）——逐存货 `{库存金额, 总账金额, 差异}` + 三总计卡；差异行红色高亮，全平显示「✔ 对账平衡」，差异提示两大常见成因（单据未出凭证 / 未结价）。
- **UI**：成本核算页新增「总账对账」页签（期间 + 对账按钮）。
- **测试**：web `gl_reconcile_flow`（带价采购到货 90 → 对账差异 90 = 未暂估的真实信号 → 手工暂估凭证借140301带存货辅助/贷应付 → **记账后**总账侧 90 → 差异归零对平）。

### 4.21 销售出库闭环（发货/退货写库存流水，与采购侧对称）

- **结构性缺口修复**：Web 销售发货此前只写执行流水 + 收入凭证，**不写库存出库流水**（`StockKind::Sale` 生产调用点仅桌面手工单——与 P3 修复前的采购到货同构）→ `so_shipment_with_stock` 同事务「发货执行行 + sale 出库流水（负数量）」；退货 `so_return_with_stock` 对称（负执行行 + 正数量回库）+ **超退防呆**（本次 ≤ 净发货 = 发货−历史退货，与采购侧 `po_receipt_sum` 对称）；品名取订单首行（多行按首行出库）。
- **成本口径**：sale 流水 `price=amount=0`——发出成本由「销售成本结转」（`stock_summary` 按计价方法回写）确定，与领料/组装同口径；收入凭证（借应收/贷收入，比例法）行为不变。
- **测试**：web `so_shipment_stock_flow`（采购入库10 → 未确认发货400 → 确认 → 发货4 后库存=6 + 收入凭证 → 超退8 拒 → 退货2 库存回 8）。

### 4.22 销售成本结转（Web 化 + 口径修正，对标金蝶存货核算-凭证生成）

- **口径修复**：`stock_summary` 的"出库"原为**全部负流水**（含领料/形态转换）——制造业账下结转会虚增主营业务成本 → 收敛为**仅 kind=sale 的销售出库**（移动加权与全月一次两分支同步；计价引擎仍跑全部行保证成本序列正确，非销售出库只消耗库存、不计入结转与回写）。
- **Web 入口**：`POST /api/cost/sales-cost {period, method, date?}`（CostOps）——借 6401 主营业务成本 / 贷 140501 库存商品（数量 + 存货辅助 + **显式发出单价 amount÷qty**：0 价入账过的存货缺 price 会触发计价引擎「首次入库必须指定单价」）；期末结价页新增「结转销售成本」按钮（口径下拉：移动加权/先进先出/全月一次），缺省日期 = 期间末日。
- **防呆**：本期无销售出库 → 不生成凭证（none 提示）；同期间已有未作废结转凭证 → 400 拒绝重复（重做先作废原凭证）。
- **连带修复**：形态转换按源存货移动加权成本**平移金额**（金蝶同口径；源无成本价拒绝）。
- **测试**：web `sales_cost_cutover`（入库无 sale → none → 发货4 → 形态转换2 → 结转 6401 = 4×9 = **36**（口径排除转换件、数量 10-4-2=4 双断言）→ 重复结转 400）；findb `business::` 9/9 回归。

### 4.23 来料检验状态机（对标金蝶质检管理，schema v24）

- **模型**：`stock_move.qc_status`（''可用 / `pending` 待检 / `quarantine` 隔离，v24 迁移补列）+ 存货档案「启用来料检验」（aux props.qc_required，档案编辑器勾选）。
- **流程**：qc_required 存货到货 → **待检入库**（响应 `qc_pending`，执行坞 toast 提示"待检入库，待质检转正"）→ 质检单三态：**全合格转正可用 / 全不合格转隔离（库存持有但不可用，仍可退货）/ 部分不合格拆分**（合格部分留在原行转正并按不合格数扣减数量金额，拆出隔离行按原单价分摊）；**逐笔串行检验**（取首笔 pending，前笔处理完自动轮到下笔）。未勾检验的存货走**事后质检**（不合格自动退货——P3 行为不变，双模式并存）。
- **可用量三口径**：`warehouse_stock` 扩展 `{qty 结存, available 可用, pending 待检, quarantine 隔离}` 四桶——分仓库库存页新增三列；**低库存预警改按可用量**（`below_safety` 过滤 qc_status=''），待检/隔离不参与。
- **测试**：web `qc_state_machine`（建档勾检验 → 到货10待检三口径断言 → 质检 partial 3：可用7/隔离3/待检0 → 第二单全不良：隔离累计8 → 未勾检验存货直通 available=结存）。

### 4.24 发票↔单据勾稽 + 进项认证（对标金蝶发票管理）

- **下推开票**：`POST /api/invoices/from-po {po_id}`（采购订单→进项发票）/ `POST /api/invoices/from-so {so_id}`（销售订单→销项发票，金额=订单整单）——金额=订单不含税/税额/价税合计、购销双方自动带出、状态=**待认证**、票面代码/号码留空后补；0 额订单 400 拒绝；权限 `VoucherNew`，采购/销售订单行「票」按钮按 `voucher_new && order_ops` 显示（会计/主管/管理员/应付会计可用）。
- **勾稽**：复用 `doc_link`——PO/SO 下游、发票上游双向可在单据链面板查看；发票列表行新增「链」按钮（`openDocChain("invoice")`，含订单与发票互查）。
- **进项认证流**：既有状态机（`pending 待认证 → verified 已认证 / rejected 已作废`）+ 既有端点 `/api/invoices/:id/status` 与页面「认证/作废」按钮保持——下推的发票落 pending，进入认证闭环。
- **测试**：web `invoice_push_and_certify`（采购链：到货→下推进票 amount90/supplier/pending → PO↔发票双向勾稽 → 认证 verified；0额拒；销售链：确认发货→销项票整单20/客户甲 + SO→发票勾稽）。

### 4.25 通知中心 + 单据流程条 + 审批快捷键（动态通知与点击直达）

- **铃铛 + 右侧抽屉**：顶栏 🔔 未读红点（>99 显 99+，新事件脉冲动画）→ 三段抽屉：**我的待办**（`collect_todos` 实时聚合，count>0 才显示，点击直达处理页）、**动态**（审计日志按可见性——有 AuditLog 权看全量、否则只看自己的操作；按服务端时钟分 今天/昨天/更早）、**全部已读**。
- **已读水位**：localStorage 记服务端 `now`（同钟同格式 ⇒ 字典序=时间序）；未读 = 水位后动态 + 待办项数；**零服务端状态**。
- **动态推送**：60s 轮询（`visibilitychange` 不可见挂起），新事件铃铛脉冲 + 轻 toast（5 分钟节流）；打开抽屉即拉取。
- **单据流程条**：报销编辑器头部横幅（审批中/已通过/已驳回 + 当前节点 + 最近动作 + 流程名）；请购/报价/收付款**列表行徽标**（`data-wftag` 占位 + `fillWfTags` 异步填充，如「流程:审批中 · 复核」）——单据走到哪一步一眼可见、免跳实例页。
- **审批快捷键**：工作流实例页 **J/K 上下选行**（首行默认选中）、**A 通过 / R 驳回**（输入框、弹窗、组合键时失效），表格顶部提示条。
- **端点**：`GET /api/notices`（Report）、`GET /api/workflows/instance-for`（Report，四类业务白名单）；findb 新增 `workbench::collect_todos`（轮询轻量版，权限与状态口径与 collect 一致）与 `workflow::instance_for`。
- **测试**：web `notices_endpoint`（结构/now 格式/水位归零/造审计动态后未读≥1/流程条 found+running+节点推进复核/无实例 found=false/非法类型400）。

### 4.26 发货通知单 + 票款勾稽（对标金蝶发货通知，下推矩阵①）

- **发货通知**：新表 `ship_notice`（pending/shipped）——销售订单行「通知」按钮（非草稿显示，数量默认=行未发量）发出备货指令；**通知 ≤ 未发量、未确认订单 400**；「待发通知」面板（so-doc 页，待发优先）+「去出库」预填执行坞；**出库成功自动完成最早一条待发通知**（`notice_fulfill_on_shipment`，逐条数量对齐留待迭代）。
- **票↔款勾稽**：订单页收款/付款生成收付款单后，自动与该订单**已下推的发票**建 `doc_link(invoice→receipt)` 边（`link_receipt_to_src_invoice`，已链跳过）——发票「链」面板直达收付款单，`doc_chain` 新增 receipt/notice 节点、`doc-links` 白名单扩 receipt。
- **测试**：web `ship_notice_flow`（未确认拒通知 → 超未发量拒 → 通知入 pending → 出库自动完成 → 下推销票+订单收款后发票上游见收付款单）。

### 4.27 界面字号调节 + 移动端适配增强

- **字号四档循环**：顶栏「Aa」按钮循环 小 90% → 标准 100% → 大 115% → 特大 130%（toast 提示当前档）；字号全为 px 硬编码（rem 改造不现实）→ **`html.zoom` 整体缩放**（Chrome/Edge/新 Firefox 支持，不支持的浏览器无害回退不缩放）；档位存 localStorage、脚本加载即应用防登录前闪烁，renderShell 时刷新按钮文字。
- **移动端增强**（≤600px 段，P2-7 基础上补三个真缺口）：① `input/select/textarea { font-size:16px }`——**iOS 聚焦防自动放大整页**（Safari 对 <16px 输入强制缩放）；② `.btn.sm { min-height:36px }`——触摸目标从 ~28px 提到 36px；③ `table.grid { font-size:14px }` 手机可读性；④ 通知抽屉手机上全宽（100vw）。
- **已有基础复核**：viewport meta、≤900px 侧栏抽屉+hamburger、≤600px topbar 换行/卡片单列/弹窗全宽、表格横向滚动、抽屉 92vw 均已具备，本轮零改动。

### 4.28 批次盘点 + 批次成本勾稽 + 批次调拨（对标金蝶批次管理，清单⑤）

- **批次盘点**（schema v25：`inv_count_line.batch_no`，空=整仓口径向后兼容）：盘点行可填批次号——**账面服务端按批次快照**（`batch_book_qty`：分仓/全仓流水净额，不信前端）；应用时差异流水带 `batch_no`（原硬编码空）、**盘盈新批次自动建档**（INSERT OR IGNORE，主仓=单据仓库）；列表行显示 `存货#批号`。UI 盘点表单加批次列（留空=整仓）。
- **批次成本勾稽**：`GET /api/inventory/batch-cost`（CostOps）——批次层价值（建账以来流水按 item+batch 聚合）vs **存货辅助账期末**（aux kind=Item），逐存货 diff 高亮 + 批次明细折叠；批次页新增「批次成本勾稽」面板。**口径声明**：两侧期间起点不同（批次侧不含期初历史、账面侧含期初），差异本身即定位信号（期初未建批次/辅助手工调整/未结算流水）。
- **批次调拨**（Web 此前只有报表无创建）：`POST /api/inventory/transfer`（Warehouse）——源仓分仓余额校验（不足 400）→ **调出（qty 负）/调入（qty 正）两条 Transfer 流水**（标准价、amount=0 计价引擎结算参与成本序列）→ **批次主仓标签改写**（`stock_batch UNIQUE(item,batch_no)` 全仓一行，分仓数量以流水分账）；批号留空 = **FEFO 近效期自动选批**（首条不足量 400 提示分批）。批次台账行「调拨」按钮 + modal（源仓默认主仓/目标仓/数量/日期）；调拨报表新增批号列。
- **测试**：web `chain5_batch_stock` 一件串三——造批次 W01×10 → 批次盘点（快照=10 断言/应用后余额 8）→ 成本勾稽（detail 含批次、Σqty=8、book/diff 列）→ 调拨 W01→W02×3（双流水断言、主仓改写 W02、总量仍 8、超额 400、FEFO 自动选中 BT500）；回归 findb `count_apply_loss_gain_and_guards` 1/1 + web `stock_count_flow`/`stock_batch_flow`/`workbench_role_matrix` 3/3。

### 4.29 MPS 主生产计划 + 粗细排产（对标金蝶 MPS/排产，清单⑥）

- **MPS 主计划**（新表 `mps_plan`）：需求 = 已确认销售订单未发量 ∪ 手工行 → **净算计划量 = 需求 − 现有库存 − 在制未完工**（防重复下达）；「运行 MPS / 最近一次 / 计划量转 MRP」；行「**下达**」一键 = open→converted（条件更新防并发）+ **同事务语义生成已下达生产订单**（账期=当前计划期、单据日期=建议交期，允许跨期；数量可改、计划量为 0 拒、重复下达 400）；交期默认今天+7。
- **粗排**（`POST /api/mps/rough`）：**件/日产能**口径（v1——工艺路线暂无标准工时字段，工时口径留待迭代，诚实声明）——本期间未完工自制订单按单据日期顺排，`need_days=ceil(open/日产能)`、逐日装载产生**按日负荷**（超载标红）；建议区间单行「应用」/「全部应用到订单」。
- **细排**（`POST /api/prod/schedule`，schema **v26**：`production_order.plan_start/plan_end`）：批量写回计划开工/完工日（**仅未完工订单**，条件更新防误写终态单）；生产订单列表投影补 plan 字段；「③ 细排结果」面板显示已排订单。
- **顺手修**：`run_at` 秒级精度同秒两次运行会在 `MAX(run_at)` 查询中混合（MPS 与 MRP 同款隐患）——均改微秒格式。
- **UI**：新页「MPS 排产」（NAV 生产制造组），三块面板 ①MPS ②粗排+负荷 ③细排结果。
- **测试**：web `mps_schedule_flow`——确认 SO 未发量6 → MPS demand=6/planned=6 → 在制4后再跑 wip=4/planned=2 → 下达生成 SC 单+重复 400 → 粗排 日产能2：MPS单1天/在制单2天/负荷非空 → 细排写回 plan_start=2026-02-01 + 空清单400 + 空需求400；回归 findb `mrp_basic`/`mrp_lot_size_and_safety_stock`/`prod_crud`/`count_apply_loss_gain_and_guards` 4/4 + web `production_issue_complete_flow`/`workbench_role_matrix` 2/2。

### 4.30 报表数字钻取 + 列偏好持久化（清单⑦，**六项清单全部收官**）

- **数字钻取**：`GET /api/reports/account-detail?account=&from=&to=`（FinReport，`report_range` 同款参数）——findb `balances::account_detail`：**期初累计 + 分录逐笔 + 运行余额**（借正贷负）；**口径与试算平衡 H-3 完全一致 = 仅已记账 `status='posted'`**（首版 `<>void` 含草稿被 H-3 揭示口径不一致，已对齐——草稿/已审未记账不进）。
- **UI**：`openAccountDetail()` 通用明细弹窗（期初行 → 逐笔 日期/凭证号/摘要/借贷/运行余额 → 合计，打印预览复用）；**二级钻取**：明细行点击直接打开凭证编辑器；试算平衡表**数据行点击即钻取**（`data-drill`，行 hover 提示）；参数透传与试算同源（同 `parse_period`）。
- **列偏好**：`autoColPrefs()` **全局自动挂载**——扫 `#main table.grid`（列数≥4）表前插「列▾」checkbox 菜单（列名取自 th 文本），隐藏走 **CSS 类 + 动态 `<style>` 规则**（`table.pc-KEY.ch-N nth-child`）——**数据异步后填的行自动继承**；localStorage `colpref:{view}#{序号}` 持久化；`renderMain` 同步挂 + `api()` 回调补挂（async 表全覆盖）；点击表外关菜单。声明：列**显隐** v1，列宽拖拽留待迭代。
- **顺手修**：明细 SQL 首版 `v.summary` 列不存在（voucher 表是 `memo`）——mem 单测复现定位。
- **测试**：findb `account_detail_smoke`（手插凭证直调）1/1；web `report_drill`——三笔记账凭证（借100/借200/贷30，每笔保存即记账走 H-3 流程）→ 本期 begin=0/3行/借300/贷30/末行余额270/凭证号记82/分录摘要 → 下期 begin=270 期初累计、无行 → 缺 account 400；回归 `trial_balance_default_posted_only_h3`（口径锚）+ `web_aux_qty_and_custom_reports` + `fin_report_vs_business_report` 3/3。

### 4.31 各岗位数据导入（平台迁移专项）+ 存货计划参数入口

- **现状复用**：`findb::imports` 原有三平台模板（金蝶/用友/通用）、CSV/Excel 双通路、科目映射、预检/执行两段——原仅覆盖会计（期初+凭证）。本专项扩到**各岗位**。
- **4 个新 kind**（复用两段框架，行级 warnings + 跳过计数）：
  | kind | 岗位 | 列 | 落库 | 鉴权 |
  |---|---|---|---|---|
  | `aux` 档案 | 销售/采购/人力 | 类型,编码,名称,备注 | `aux_entity` upsert（**重码/坏类型跳过**） | AuxEdit |
  | `item` 存货 | 仓管 | 编码,名称,保质期天,安全库存 | aux(item)+props + `item_plan` 安全库存 | Warehouse |
  | `account` 科目 | 会计 | 编码,名称,类别,方向,备注 | 科目表（**类别空按编码首位推** 1资产/2负债/3权益/4成本/5,6费用） | AccountEdit |
  | `opening_stock` 期初库存 | 仓管 | 存货,仓库,数量[,单价,批次,生产日期,备注] | **其他入库数量流水（账期=建账首期）+ 批次建档（生产日+档案保质期推失效日）**；存货未建档跳过 | Warehouse |
- **口径声明**：期初库存**不生成凭证**——金额与存货科目期初由「科目期初」导入负责，双侧各管一段避免重复记账；档案类**逐条幂等**（中断可重导，重码静默跳过），与期初的整批事务有意不同。
- **per-kind 鉴权**：页面入口不变，动作按 kind 分流（`import_perm`：voucher/begin→VoucherNew、account→AccountEdit、aux→AuxEdit、item/opening_stock→Warehouse、未知 kind 400）。
- **模板下载**：`GET /api/import/template?kind=`（CSV 带 BOM Excel 直开；6 类列头+示例行）；页面 kind 下拉分组（基础资料/期初/凭证）+ 列头提示随类型切换 + 「下载模板」按钮。
- **B 项·存货计划参数入口**（补断链）：`GET/POST /api/item-plan`（Warehouse）——存货档案编辑器新增 **安全库存/前置期/批量** 三字段（回显 GET、随档案同存 POST）——低库存预警报表 ↔ 设置入口闭环（此前 `item_plan` 只有表和 MRP 逻辑、**无 Web 入口**）。
- **测试**：web `import_master_data`——模板 BOM/列头/未知 kind 400 → aux 导入 ok2+skip2（重码/坏类型）+幂等重导 ok0/skip4 → item 保质期入 props + 安全库存入 item_plan → account 类别推断建档 → **未知 kind 400** → opening_stock 批次建档（W09、失效日=生产+30、余额40）+ **凭证零生成断言**；既有导入回归 `import_*` 全量。

### 4.32 独立存货档案页（C 选项）+ 往来期初按单据明细（导入 v2）

- **存货档案页**（NAV 库存组，页=warehouse 查看、编辑服务端 AuxEdit 拦）：`GET /api/items/master` 一次聚合四源——`aux(kind=item)` 全量 + `item_plan`（安全库存/前置期/批量）+ **库存现量**（流水逐行汇总）+ **主单位**（item_unit）；列表含 编码/名称/单位/现量/安全库存/前置期/保质期/质检/状态/备注，**低库存红显**（现量<安全库存）、关键字搜索、只看低库存开关、行「编辑」复用通用档案编辑器（构造 AuxEntity 形状）、新建同款；`openAuxEditor` 保存后改 `rerenderView(state.view)`（原硬编码 aux 页——通用化后从存货档案页编辑不再跳页）。
- **往来期初按单据**：新表 `arap_opening`（ar/ap、客商、单据号/日期、金额——纯新表）；导入 kind `arap_opening`（perm VoucherNew，类型/日期/金额行级校验、**同 kind+客商+单据号幂等跳过**）+ 模板 + 期初组下拉；账龄页（viewSettle）新增「往来期初明细」区块（列表 + **应收/应付分列合计** + 删除 + 去导入）。
- **账龄覆盖**：`settle::aging` **单点注入**影子 AgingItem（按科目方向 1↔ar、2↔ap 过滤、单据期间≤upto、doc_no 标「期初」）——**不碰 `open_entries`/FIFO 零核销风险**。
- **口径声明**：期初明细为**影子挂账**——进账龄展示与管理，**暂不参与自动核销**（收期初款的 FIFO 核销留 v2，需 settle 对伪 entry 的支持）；金额总额仍以科目期初为准，本表承载逐单欠款信息。
- **测试**：web `items_master_and_arap_opening`——arap 导入 ok3/skip1 + 幂等 ok0/skip4 → 列表合计 8000/2000 → **1122 账龄含期初客商 Q01** → 删除后 2 行；存货建档（导入保质期45）+ item-plan（safety30/lead3/lot10）+ 期初数量 25 → master 聚合字段全断言（shelf/safety/lead/lot/qty/disabled）；回归 web settle 3/3 + findb aging 1/1。

### 4.33 Web 平台安全中心（口令策略可配 + 登录审计 + 持久锁定）

- **背景**：Web 登录是**平台层**（认账号库 realm）——此前口令策略硬编码默认值（不可配）、登录只走内存限流（重启即忘、不留审计），与桌面端账套级策略/锁定语义不一致。
- **策略可配**：`GET/POST /api/security/policy`（仅平台管理员；值域校验：长度 1-64、失败次数 1-20、锁定时长 1-1440 分、空闲登出 0-1440 分、有效期 0-3650 天）→ 存 realm `realm_option`；**改密校验 / 空闲登出 / 登录锁定统一走该策略**（`WebState::policy()`，读取失败回退默认，不因策略库异常阻断登录）。
- **登录审计**：realm 新表 `realm_login_attempt`（只留最近 500 条）+ `GET /api/security/login-attempts?username=&limit=`；成功登录亦留一条（先 `unlock` 清失败再记成功，成功记录不被清理）。
- **持久锁定**：`realm_user.locked_until`（老库启动自动补列）+ `GET /api/security/locked-users` + `POST /api/security/unlock`——连续失败达策略阈值写锁，**跨进程/重启生效**；锁定期间口令正确也 429（不再做昂贵 argon2 校验）；解锁同时清空失败计数。
- **UI**：「系统 → 安全中心」新增两块（仅平台管理员可见）——**口令策略表单** + **平台登录审计**（锁定账号 🔒 红标 + 一键解锁）；账号管理/账套成员锁定解锁保持原样。
- **测试**：web `web_security_center`——默认策略 → 保存 max_fail=3 → 非法值 400 → 3 次错锁定 → 正确口令也 429 → 审计含失败记录 → 锁定名单可见 → 解锁后登录成功 + 审计含成功记录 → 非管理员 403 → 解锁陌生账号 404。

---

## 5. 关键设计约定

| 主题 | 约定 |
|------|------|
| **金额类型** | 全部 `rust_decimal::Decimal` 运算，零误差；SQLite 存为 TEXT，**绝不 SQL SUM()**，一律 Rust 侧累加；除法一律 `checked_div`（除零返 `None`，**禁止静默 0**，由调用点显式 `expect`（判零守卫）/`unwrap_or(ZERO)`（展示兜底）/报错传播） |
| **余额符号** | **正 = 借，负 = 贷**（全引擎统一，UI 层再按 (方向, 绝对值) 展示） |
| **科目级长** | 4-2-2-2（1001 / 100101 / 10010101 / 1001010101）；建账时设定后不可改 |
| **余额口径（H-3 定案）** | 余额表不物化、实时计算 = 期初余额 + **已记账分录**；科目余额表 / 三大报表 / 试算平衡同此口径（默认 `posted_only=true`）；草稿与已审核未记账一律不入余额。**例外**：期末结转类操作（结转损益/年末结转/预检）按含未记账取数——结转凭证本身是草稿，靠含草稿口径判重防重复结转；账簿提供「只含已记账」开关，取消勾选按「含未记账（排除作废）」查看，行集与期初同口径 |
| **借贷平衡（M-15 定案）** | 分录金额**逐条量化到 2 位（分）**后借贷合计严格相等（`Voucher::balanced`），与落库 `money_param` 口径完全一致——差额 0.005 的「半分尾差」不被放过（否则入库后真差 0.01）。试算平衡按 round2 判定，差额不足 1 分视为平衡。固定口径，不可配置 |
| **凭证状态** | 草稿 → 已审核 → 已记账；已记账可**作废**不能删除；系统生成凭证带来源标记（自动转账/调汇/业务/模板） |
| **存货出库** | 出库行数量为负、单价金额置零，成本由计价引擎（移动加权/FIFO）计算 |
| **附件** | ≤256KB 内联 SQLite BLOB，大文件落账套同目录 `.attachments/`，SHA-256 完整性校验 |
| **权限** | **12 角色**（系统管理员/财务主管/会计/出纳/审核人/订单专员/仓管员/**应收会计/应付会计/成本会计/生产计划员**/只读）+ 细粒度权限项 + 数据范围；**身兼多职**：账号可设主岗位 + 兼任岗位（权限取并集，出纳签字与会计核心仍互斥）；**订单作业 / 仓储作业 / 生产作业 / 成本作业 / 价格查看 / 价格修改** 独立权限位（岗位与科目维护解耦）；**财务报表独立权限 `FinReport`**（账簿/三大报表/试算/资金/成本/预算/发票/银行对账——订单专员、仓管员、生产计划员不可见，业务报表仍走 `Report`，对标金蝶报表按角色授权）；**价格字段权限**：无「价格查看」者订单列表与套打服务端裁掉价格（数量保留），无「价格修改」者改单旧价保留、新行置零；**权限调整（建号 / 改角色 / 权限矩阵 / 数据范围 / 删号）与身份操作（重置口令 / 重置设备 / 解锁停用）仅套内管理员可操作**（持有 `UserManage` 的非管理员一律 403——口令为全局口令，重置即跨账套身份变更）；**导出（`Export`）仅管理员与财务主管**，其余账号只能打印预览 |
| **账号模型** | **二元：管理员 / 普通账号**（无"平台账号"概念）。管理员：新建账套、开通账号、赋予岗位权限、进入全部账套（临时身份不落套内记录）；普通账号：由管理员开通并邀请入套，只参与业务与财务。五层闸按此分层：开号 / 建套 / 邀请入套 / 改授权 / 身份操作（重置口令·设备·解锁）逐层收敛至管理员；**存量普通账号名下的账套于每次启动幂等迁移至最早管理员名下**（`migrate_book_owners_to_admin`，并补该管理员的套内 Admin 成员行） |
| **审计** | 所有写操作（建账/凭证增删改/审核/记账/结账/登录/锁定/超时登出）全部进 `audit_log` |
| **备份** | 复制 `.fbk` 即可；UI 内「备份恢复」一键备份与按备份恢复 |

---

## 6. 账号开通方式（默认管理员，其余由管理员开通）

新账套内置一个**默认管理员账号**：

| 用户 | 说明 |
|------|------|
| 内置管理员 | 账号 `admin`，默认口令 `admin123`，**首次登录强制改密**；获得全部权限 |
| 其余账号 | 由管理员在「系统 → 用户权限 → 用户管理」开通 |

- **默认管理员**：每个账套建好即内置 `admin` 账号，用默认口令首次登录后必须修改。
  旧版「首登建号」遗留的空用户账套，打开时自动补种该默认管理员。
- **其余账号由管理员开通**：管理员进入「系统 → 用户权限 → 用户管理」，点击「新增用户」填写用户名、
  姓名、角色、初始口令与数据范围即可。新建账号默认**强制首次登录改密**、**自动绑定当前设备**（见 §4.5）。
- 普通账号登录设备受绑定限制，换设备需管理员「重置绑定」。
- 管理员可随时在「用户管理」停用账号、重置口令、重置设备绑定、解锁锁定账号、查看/编辑数据范围。

> 一键重置账套内所有账号口令为 `admin123`（常用于交接 / 忘密）：
> ```bash
> cargo run -p findb --release --example reset_pwd -- path/to/company.fbk
> ```

---

## 7. 命令行工具（debug / 运维）

| 工具 | 用法 | 作用 |
|------|------|------|
| `create` | `cargo run -p findb --example create --release -- <path>` | 创建空账套（118 个默认科目、1 个内置管理员账号 `admin`） |
| `seed` | `cargo run -p findb --example seed --release -- <path>` | 导入现金流量项目、演示辅助档案 |
| `reset_pwd` | `cargo run -p findb --example reset_pwd --release -- <path>` | 重置所有用户密码为 `admin123` |

---

## 8. 测试与质量

```bash
cargo test --workspace
#   fincore（纯计算引擎）........ 93 passed
#   findb（持久化 + DAO + 安全）.. 153 passed
#   e2e core_loop（核心闭环）..... 21 passed
#   e2e ext_loop（扩展闭环）....... 7 passed
#   finweb（REST API）............ 11 passed
#   ──────────────────────────────────
#   合计                     285 passed, 0 failed
```

- `crates/findb/tests/core_loop.rs`（21 个）：建账 → 科目规则 → 期初 → 凭证校验与状态机 → 账簿 → 余额表 → 三大报表 → 结转损益 → 结账/反结账 → 年末结转 → 备份 → 审计日志。
- `crates/findb/tests/ext_loop.rs`（7 个）：
  - **t90** 资产卡片 → 计提折旧 → 折旧凭证 → 台账净值 → 清理
  - **t91** 对账单录入 → 自动勾对 → 余额调节表
  - **t92** 往来核销 → 核销凭证 → 账龄分析
  - **t93** 期末调汇 → 自动转账 → 检查清单
  - **t94** 存货出入库 → 结转成本（移动加权）→ 工资计提 + 个税累计预扣 → 报销状态机
  - **t95** 预算编制与执行 → 多维损益 → 自定义报表 → 凭证模板 → 摘要热度 → 附件增删
  - **t96** 口令策略 → 连续错 5 次锁定 → 解锁 → 会话空闲超时

---

## 9. 已知限制

- **数据范围（DataScope）**：用户级数据范围已在安全中心配置、登录链路生效，但各账簿界面的**逐模块过滤**尚未全部接入（界面内有灰字提示）
- **打印**通过系统默认浏览器预览 + Ctrl/Cmd+P 完成，未对接虚拟打印驱动；导出 Excel/CSV 仅管理员与财务主管可用（普通账号仅能打印）
- **跨账套查询**、**合并报表**未实现
- 附件哈希为 SHA-256 完整性校验（防误删损坏），不防篡改
- 账号设备绑定按"一台账号绑一台设备"设计，管理员可在「用户管理」重置；**不要把同一个 `.fbk` 放在 NFS/SMB 网络盘被多台电脑并发直开**（WAL 在网络文件系统上不稳定，见 §1.1）
- **质检单流程**（对标金蝶质检管理）**已落地基础版**（§4.17：来料质检单、合格判定、不合格自动按订单单价退货冲减库存）；待检状态机（入库先入待检区）、工序质检与不良品隔离库位联动为后续迭代
- **批次成本勾稽 / 批次盘点 / 批次调拨联动**待立项：批次主数据、自动批号、FEFO 推荐、临期预警、库位主数据与登记已落地（见 §4.1「批次与库位」）；批次级成本分摊与批次盘点维度为后续迭代
- **销售成本结转 Web 入口与口径**待立项：`stock_cost_voucher`（借6401/贷1405）仅桌面可触发、Web 无按钮；且其数据源 `stock_summary` 的"出库"= 期间全部负流水（**含领料/形态转换出**）——制造业账下直接结转会虚增主营业务成本，需按 kind 过滤（sale-only 或分离在产品）后再开放 Web 按钮
- **款到发货（先款后货）开关**待立项（用户方案 B）：发货端点校验订单已收金额 ≥ 本次发货金额否则 400；粒度二选一——客户级（往来档案 props）或账套全局参数

---

## 10. 目录结构

```
finbook/
├── Cargo.toml                    # workspace
├── .cargo/config.toml            # x86_64-pc-windows-gnu 链接器配置
├── crates/
│   ├── fincore/                  # 纯计算：科目、凭证、引擎、报表、个税、存货计价（无 IO）
│   ├── findb/                    # SQLite 持久化：DAO + security + attach + summaries + template
│   ├── finui/                    # egui + eframe 桌面 UI（28 个功能界面）
│   └── finbook/                  # 二进制入口（main.rs + eframe::run_native）
└── target/
    ├── release/finbook                  # Linux x64 (~17 MB)
    └── x86_64-pc-windows-gnu/release/finbook.exe  # Windows x64 (~12 MB)
```

---

## 10.1 Windows 保留设备名适配（aux 模块重命名）

本仓库在推送到 GitHub 前做过一次**模块重命名**，原因与影响说明如下：

- **原因**：原源码中有两个文件名为 `aux.rs`（`crates/fincore/src/aux.rs` 与 `crates/finui/src/views/aux.rs`）。
  `aux` 是 Windows 的保留设备名（类似 `con`/`prn`/`nul`），git 在 Windows 上默认开启 `core.protectNTFS`，
  会拒绝索引这类路径，导致 `git add` 直接失败、无法提交/推送。
- **处理**：
  - `crates/fincore/src/aux.rs` → **`crates/fincore/src/auxiliary.rs`**，模块声明 `pub mod auxiliary`；
  - `crates/finui/src/views/aux.rs` → **`crates/finui/src/views/aux_view.rs`**，模块声明 `pub mod aux_view`；
  - 同步更新了 4 处引用：`crates/fincore/src/lib.rs`、`crates/findb/src/mgmt.rs`（测试）、
    `crates/finui/src/views/mod.rs`、`crates/finui/src/views/aux_view.rs`。
- **验证**：重命名后 `cargo check --workspace` 编译通过（无新增警告）。
- **克隆注意事项**：在 Windows 上克隆 / 编译本项目时，请以 `auxiliary.rs` / `aux_view.rs` 为准，
  不要再把文件改回 `aux.rs`，否则会再次触发同一问题。对外 API 不变（`fincore::auxiliary::AuxEntity`、
  `fincore::auxiliary::validate_aux` 等），`findb::auxs` 模块名本就合法、未受影响。

---

## 11. 重新交叉编译 Windows 包

```bash
sudo apt-get install mingw-w64
rustup target add x86_64-pc-windows-gnu
cargo build --release --target x86_64-pc-windows-gnu -p finbook
# 产物：target/x86_64-pc-windows-gnu/release/finbook.exe
# GUI 子系统：由 crates/finbook/build.rs 注入 -mwindows，双击运行无黑色控制台窗口
```

---

## 12. 截图集

| # | 页面 | 截图 |
|---|------|------|
| 01 | 登录页 | ![](screenshots/01_login.png) |
| 02 | 首页 / 工作台 | ![](screenshots/02_dashboard.png) |
| 03 | 会计科目 | ![](screenshots/03_accounts.png) |
| 04 | 凭证查询 | ![](screenshots/04_voucher_list.png) |
| 05 | 填制凭证 | ![](screenshots/05_voucher_new.png) |
| 06 | 期初建账 | ![](screenshots/06_begin.png) |
| 07 | 账簿查询（明细账） | ![](screenshots/07_ledger.png) |
| 08 | 科目余额表 | ![](screenshots/08_balance.png) |
| 09 | 资产负债表 | ![](screenshots/09_reports.png) |
| 10 | 期末处理 | ![](screenshots/10_period_end.png) |
| 11 | 辅助档案（供应商） | ![](screenshots/11_aux.png) |
